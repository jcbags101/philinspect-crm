import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Card } from "@/components/ui/card";
const label = (value: string) => value.replaceAll("_", " ").replace(/\b\w/g, (c) => c.toUpperCase());
interface Row { id: string; title: string; companyName: string; assignedToName: string | null; status: string; location: string | null; scheduledAt: Date | null; deletedAt: Date | null }
export function InspectionTable({ inspections }: { inspections: Row[] }) { return <Card className="overflow-hidden p-0"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead><tr className="border-b bg-muted/30 text-left text-xs text-muted-foreground"><th className="px-4 py-3">Inspection</th><th className="px-4 py-3">Company</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Inspector</th><th className="px-4 py-3">Schedule</th><th className="px-4 py-3">Location</th></tr></thead><tbody>{inspections.map((item) => <tr className="border-b last:border-0" key={item.id}><td className="px-4 py-3"><Link className="font-medium hover:text-primary hover:underline" href={`/inspections/${item.id}`}>{item.title}</Link>{item.deletedAt && <p className="text-xs text-destructive">Archived</p>}</td><td className="px-4 py-3 text-muted-foreground">{item.companyName}</td><td className="px-4 py-3"><Badge variant="outline">{label(item.status)}</Badge></td><td className="px-4 py-3 text-muted-foreground">{item.assignedToName ?? "Unassigned"}</td><td className="px-4 py-3 text-muted-foreground">{item.scheduledAt?.toLocaleString("en-PH") ?? "—"}</td><td className="px-4 py-3 text-muted-foreground">{item.location ?? "—"}</td></tr>)}{inspections.length === 0 && <tr><td className="px-4 py-10 text-center text-muted-foreground" colSpan={6}>No inspections found.</td></tr>}</tbody></table></div></Card>; }
